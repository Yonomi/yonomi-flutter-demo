import 'dart:ui';

// A place to define our app's color palettes which ideally should be decoupled
// from its UI components and their theme(s),
// i.e.. Color names should not contain things such as BottomAppBar's text color,
//  or Background container text color
class ColorConstants {
  static const Color yonomiYellow = Color(0xFFFBE337);

  static const Color darkBackground = Color(0xFF1E1F29);

  static const Color lighterDarkBackground = Color(0xFF484950);

  static const Color lightGreyTextColor = Color(0xFFC9C9CC);

  static const Color redAlert = Color(0xFFEE183F);

  static const Color transparent = Color(0x00000000);
}
