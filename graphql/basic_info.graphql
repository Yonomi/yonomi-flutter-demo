query basicInfo {
  # "me" is the entry point to any data specific to the current user
  me {
    id
    firstActivityAt
    lastActivityAt
  }

  # basic information about the tenant
  tenant {
    id
    displayName
  }
}
